<div class="cont-prod">
  {{#each products}}
  <div class="product">
    <img src="{{thumbnails}}" alt="{{title}}">
    <h2>{{title}}</h2>
    <p>{{description}}</p>
    <p>${{price}}</p>
    <p>Cantidad: {{stock}}</p>
  </div>
{{/each}}
</div>



<script src="/socket.io/socket.io.js"></script>
